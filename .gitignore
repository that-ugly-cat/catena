__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/

# segreti: la chiave Zotero non entra nel repo, mai
.env
*.env

# materiale di prova con contenuti altrui
spike/*.docx
!spike/catena-spike.docx
scratch/

# config locale dell'editor
.claude/launch.json
data/

# Third-party source cloned at deploy time (see DEPLOY.md)
vendor/

# SPEC.md stays local: it carries the security analysis and the discarded
# options, which do not belong in a public repository (house convention).
SPEC.md
