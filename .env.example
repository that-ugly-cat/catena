# catena — variabili d'ambiente
# Il segreto dei JWT: l'avvio fallisce se manca, di proposito.
JWT_SECRET=

# Dove sta il database (un file solo).
CATENA_DB=data/catena.db

# URL pubblico, usato per costruire gli endpoint MCP mostrati nel profilo.
PUBLIC_URL=https://catena.borant.eu
